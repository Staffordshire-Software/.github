import { describe, expect, it } from 'vitest';

// Bootstrap smoke test — replace with real tests as the product grows.
// Its job is to keep `npm test` meaningful (and failing-capable) from day one.
describe('{{REPO_SLUG}}', () => {
  it('has a working test runner', () => {
    expect(1 + 1).toBe(2);
  });
});
